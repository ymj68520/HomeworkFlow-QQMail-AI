# 性能优化方案 - 全面重构

## 问题诊断

### 当前性能瓶颈

| 问题 | 当前实现 | 性能影响 |
|------|---------|---------|
| IMAP连接开销 | 每次页面切换都重新连接 | 1-3秒延迟 |
| 数据库查询 | 每条记录单独查询 (N+1问题) | 100条=200次查询 |
| UI渲染 | 逐行添加，多次重绘 | 卡顿感 |
| 批量操作 | 操作后完全重新加载 | 不必要的等待 |
| 下拉菜单重建 | 每次页面加载都重建 | 不必要的开销 |

## 优化架构

### 1. 数据层重构

#### 1.1 持久连接管理器
```python
class ConnectionManager:
    """管理IMAP持久连接和自动重连"""
    - 保持长连接
    - 自动心跳检测
    - 断线自动重连
    - 连接池管理
```

#### 1.2 批量数据库查询
```python
def get_submissions_by_uids(uids: List[str]) -> Dict[str, Submission]:
    """批量查询: WHERE uid IN (...)"""
    - 一次查询获取所有记录
    - 返回字典便于快速查找
    - 预加载关联数据 (JOIN)
```

#### 1.3 本地数据缓存
```python
class DataCache:
    """三层缓存策略"""
    - L1: 当前页数据 (内存)
    - L2: 前后页预加载 (内存)
    - L3: 数据库持久化
```

### 2. 业务逻辑层优化

#### 2.1 智能数据加载
```python
class SmartDataLoader:
    """智能数据加载器"""
    - 首次加载: 快速显示基本信息
    - 后台补充: 异步加载详细信息
    - 预加载: 后台预取相邻页
    - 懒加载: 点击时才获取邮件正文
```

#### 2.2 增量更新机制
```python
def smart_refresh(changed_ids: List[int] = None):
    """智能刷新: 只更新变化的部分"""
    - changed_ids为None: 完全刷新
    - changed_ids为空列表: 仅更新UI状态
    - changed_ids有值: 仅更新指定记录
```

#### 2.3 批量操作优化
```python
class BatchOperationManager:
    """批量操作管理器"""
    - 使用事务批量更新
    - 进度回调
    - 错误处理和回滚
    - 完成后增量更新UI
```

### 3. UI层优化

#### 3.1 批量渲染
```python
def bulk_update_table(data: List[Dict]):
    """批量更新表格"""
    - 一次性设置行数
    - 批量创建所有item
    - 单次重绘
```

#### 3.2 异步加载指示
```python
class LoadingIndicator:
    """加载状态指示器"""
    - 骨架屏
    - 进度条
    - 渐进式加载
```

## 实施步骤

### 阶段1: 数据层重构 (核心优化)
- [ ] 创建 `ConnectionManager` 持久连接管理
- [ ] 实现批量数据库查询方法
- [ ] 创建 `DataCache` 缓存系统

### 阶段2: 业务逻辑优化
- [ ] 重构 `SmartDataLoader` 
- [ ] 实现增量更新机制
- [ ] 优化批量操作流程

### 阶段3: UI层优化
- [ ] 实现批量表格渲染
- [ ] 添加加载状态指示
- [ ] 优化下拉菜单更新策略

### 阶段4: 集成和测试
- [ ] 集成所有优化组件
- [ ] 性能测试和调优
- [ ] 边界情况处理

## 预期效果

| 操作 | 当前耗时 | 优化后耗时 | 提升 |
|------|---------|-----------|------|
| 首次加载 | 5-10秒 | 1-2秒 | 70-80% |
| 页面切换 | 3-5秒 | <0.5秒 | 90% |
| 批量回复 | 5-10秒 | 2-3秒 | 60-70% |
| 批量下载 | 10-30秒 | 5-10秒 | 50-60% |
